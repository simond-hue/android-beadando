<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Felhasználónév"
        android:textSize="20dp"
        android:id="@+id/felhTextView"></TextView>

    <EditText
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/felhTextView"
        android:id="@+id/felhEditText"
        ></EditText>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Jelszó"
        android:textSize="20dp"
        android:id="@+id/jelszoTextView"
        android:layout_below="@+id/felhEditText"></TextView>
    <EditText
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/jelszoTextView"
        android:id="@+id/jelszoEditText"
        android:inputType="textPassword"></EditText>
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Belépés"
        android:id="@+id/loginBtn"
        android:layout_below="@id/jelszoEditText"
        android:layout_centerHorizontal="true">
    </Button>
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/regBtn"
        android:text="Regisztráció"
        android:layout_below="@id/loginBtn"
        android:layout_centerHorizontal="true">
    </Button>
</RelativeLayout>